import pandas as pd

import torch
import torchvision
from torch.utils.data import Dataset, DataLoader

config = {

    "train": "train.csv",
    "val": "val.csv"
}

def read_file(path):
    return 0
    